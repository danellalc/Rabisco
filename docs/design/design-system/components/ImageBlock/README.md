A imagem é sempre um bloco sozinho na linha, largura em porcentagem da coluna — nunca em pixel. Selecionada, o contorno é `acc` em 2px com `outline-offset` (nunca `border`, que empurraria o layout). A alça é um quadrado de 12px visíveis dentro de uma área de toque de 32px, com fundo `bg` e borda de 1,75px em `acc` — um contorno, não um preenchimento, mesmo aqui. Subindo, a imagem inteira cai para opacidade 0,5; não existe barra de progresso nem spinner. Falhou o upload: a imagem some, ponto — o aviso é o toast `Não deu pra enviar a imagem`, não um estado visual neste componente.
